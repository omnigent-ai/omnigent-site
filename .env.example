# Resend API key used by app/api/waitlist/route.js to send waitlist
# confirmation emails. Create one at https://resend.com/api-keys and verify
# the sending domain (omnigent.ai) under Domains before using it in
# production. In Vercel, set this in Project Settings -> Environment
# Variables for each environment (Preview/Production).
RESEND_API_KEY=
