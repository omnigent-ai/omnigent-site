import { pageMeta } from "@/lib/og";
import InstallCommandTabs from "@/components/InstallCommandTabs";
import ExtrasCommandTabs from "@/components/ExtrasCommandTabs";
import MacDownloadButton from "@/components/MacDownloadButton";

export const metadata = pageMeta(
  "Install",
  "Install the Omnigent CLI with the installer script, uv, pip, or Homebrew, then grab the macOS desktop app.",
  {
    eyebrow: "Quickstart",
    path: "/quickstart/install",
  },
);

# Install

Install the CLI, set up your credentials, and launch your first agent. When
it's running, you can add the native macOS app for a full UI.

## Requirements

- **Python 3.12+**
- **Node.js 22 LTS** and npm
- **tmux**

## Install Omnigent

<InstallCommandTabs />

Run the same command to upgrade an existing installation.

### Optional integrations

The base install covers everyday use. Extra integrations (model providers,
sandboxes, additional harnesses, and storage backends) are opt-in at install
time. Add them with the same tool you used to install:

<ExtrasCommandTabs />

Homebrew can't pass Python extras; install the base formula with `brew`, then add extras with `uv` or `pip`.

Available extras:

- **Model providers:** `databricks`, `bedrock`, `vertex`
- **Sandbox providers:** `modal`, `daytona`, `boxlite`, `cwsandbox`, `e2b`,
  `openshell`, `kubernetes`
- **SDK harnesses:** `antigravity`, `copilot`, `cursor`, `agents-sdk`
- **Storage and memory:** `s3`, `hindsight`

## Set up credentials

```
omni setup
```

The wizard detects credentials in your environment and prompts for
any that are missing. See [Models & Credentials](/docs/build/models#credentials)
for the full reference.

## Launch Debby

Try your install with Debby, a built-in multi-AI agent that sends every
question to both Claude and GPT and lets them debate.

```
omni debby
```

This launches a web UI and prints the server URL and port. Ask something
like `What came first, the chicken or the egg?` and watch the models
challenge each other. See the [Debby reference](/docs/use/builtin-agents/debby)
for more.

## Add the macOS app

The macOS app adds a full UI and session history on top of the CLI you just
installed. It's optional; everything above works from the terminal.

<MacDownloadButton />

Open the app and enter a server URL. `omni debby` prints one when it starts
the web UI, usually `http://localhost:6767`. If that port is taken, the server
uses the next one, so check the terminal output for the actual URL. Paste it
into the app to connect.

Then pick **Debby** from the agent picker and ask it anything.

## Uninstall

`omni uninstall` removes Omnigent while preserving your data by default. With
no destructive flag, it runs a dry-run preview and prints what it would do
without changing anything:

```
omni uninstall
```

To actually remove the CLI and the installer-managed PATH entries, add
`--yes`. Your local history, credentials, and projects stay in place:

```
omni uninstall --yes
```

To also delete Omnigent state under `~/.omnigent`, add `--purge`. Omnigent
archives that state to a backup outside the target (under
`~/.omnigent-backups/`) and prints a restore command before deleting; pass
`--no-backup` to skip the archive. Your `~/omnigent` workspace is kept unless
you also pass `--purge-workspace`:

```
omni uninstall --purge --yes
```

You can scope the run to one or more targets: `cli`, `state`, `desktop-data`,
or `all`. Other flags cover less common cases: `--dry-run` to force a preview,
`--json` for machine-readable output, `--force` to stop stubborn processes and
override tamper refusals, and `--modify-external-config` to allow marker-scoped
edits to third-party config files.

### Standalone fallback

If the installed wheel is broken or `omni` is not on your `PATH`, run the
standalone script instead. Add `--yes` to perform the previewed cleanup:

```
curl -fsSL https://raw.githubusercontent.com/omnigent-ai/omnigent/main/scripts/uninstall_oss.sh | sh
```
