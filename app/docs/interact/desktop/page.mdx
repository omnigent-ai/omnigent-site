import { pageMeta } from "@/lib/og";
import {
  MACOS_DOWNLOAD_URL,
  MACOS_X64_DOWNLOAD_URL,
  WINDOWS_DOWNLOAD_URL,
  LINUX_DEB_DOWNLOAD_URL,
  LINUX_APPIMAGE_DOWNLOAD_URL,
} from "@/components/links";
import MacDownloadButton from "@/components/MacDownloadButton";

export const metadata = pageMeta(
  "Desktop App",
  "The Omnigent desktop app brings the full web UI to a native window, with notifications, a dock badge, and multiple windows.",
  {
    eyebrow: "Interact",
    path: "/docs/interact/desktop",
  },
);

# Desktop App

The Omnigent desktop app gives you the full
[web UI](/docs/interact/web-ui) in a native window, plus OS-level features
a browser can't provide: notifications when your agent needs attention, a dock badge
showing how many sessions are waiting, and multiple independent windows.

<MacDownloadButton />

{/* eslint-disable-next-line @next/next/no-img-element */}

<img
  src="/images/docs/desktop-app.png"
  alt="Omnigent native desktop app showing the session chat and file workspace"
  style={{ width: "100%", borderRadius: "8px" }}
/>

## Why use it over the browser?

- **OS notifications.** Get notified when an agent finishes, asks for input, or a runner disconnects. Notifications show the first few lines of the agent's message and only fire for sessions you're not actively viewing.
- **Dock badge.** See at a glance how many sessions need your attention, without switching to the app.
- **Multiple windows.** Open additional windows with `Cmd+N` to watch multiple sessions or servers side by side.

Everything else (sessions, file editor, inline comments, collaboration) works exactly the
same as the web UI.

## Embedded browser

The desktop app includes an embedded browser pane your agent can drive directly.
Five framework-owned tools are always available to every agent — you don't declare
them in your agent YAML:

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>What it does</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>browser_navigate</code>
      </td>
      <td>
        Opens or navigates the pane to a URL, auto-opening the pane if it isn't
        open yet.
      </td>
    </tr>
    <tr>
      <td>
        <code>browser_snapshot</code>
      </td>
      <td>
        Captures an accessibility-tree snapshot: a <code>snapshot_id</code>, the
        current URL and title, and a YAML-shaped tree of interactive elements,
        each tagged with a <code>[ref=N]</code> id.
      </td>
    </tr>
    <tr>
      <td>
        <code>browser_click</code>
      </td>
      <td>
        Clicks an element by <code>ref</code> (preferred) or CSS{" "}
        <code>selector</code>. Pass the <code>snapshot_id</code> alongside a{" "}
        <code>ref</code> so a stale snapshot is rejected with a precise error.
      </td>
    </tr>
    <tr>
      <td>
        <code>browser_type</code>
      </td>
      <td>
        Focuses an input and types <code>text</code> into it, identified by{" "}
        <code>ref</code> or <code>selector</code>.
      </td>
    </tr>
    <tr>
      <td>
        <code>browser_screenshot</code>
      </td>
      <td>
        Captures a PNG of the pane for visual inspection. Prefer{" "}
        <code>browser_snapshot</code> when picking elements to act on —
        screenshots carry no refs.
      </td>
    </tr>
  </tbody>
</table>

Refs from a snapshot are far more stable than CSS selectors against generated class
names and Shadow DOM, so the usual flow is: `browser_navigate`, then `browser_snapshot`
to see what's on the page, then `browser_click` / `browser_type` using the refs it
returned.

Because the browser runs inside the desktop window, these tools require the Omnigent
desktop app to be running with the session open. If no desktop window is subscribed,
the action fails cleanly with `browser action timed out — is the session open in the
Omnigent desktop app?` rather than hanging.

## Prerequisites

- **A running server.** The [server](/docs/deploy/overview) stores your sessions and serves the UI the desktop app connects to; run it locally with `omni server start` or point the app at a deployed URL.
- **At least one runner.** A [runner](/docs/deploy/overview) is the process that actually executes your agent. If you have [cloud sandbox hosts](/docs/deploy/cloud-sandbox-host) set up, you'll be able to launch them from the app. Otherwise, register your machine by running `omni host` from your CLI.

## Install

<table>
  <thead>
    <tr>
      <th>Platform</th>
      <th>Format</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>macOS</td>
      <td>
        <code>.dmg</code>
      </td>
      <td>
        <a href={MACOS_DOWNLOAD_URL}>Download for Apple silicon</a> ·{" "}
        <a href={MACOS_X64_DOWNLOAD_URL}>Download for Intel</a>
      </td>
    </tr>
    <tr>
      <td>Linux</td>
      <td>
        <code>.AppImage</code> or <code>.deb</code>
      </td>
      <td>
        <a href={LINUX_DEB_DOWNLOAD_URL}>Download .deb</a> ·{" "}
        <a href={LINUX_APPIMAGE_DOWNLOAD_URL}>Download .AppImage</a>
      </td>
    </tr>
    <tr>
      <td>Windows</td>
      <td>
        Installer (<code>.exe</code>)
      </td>
      <td>
        <a href={WINDOWS_DOWNLOAD_URL}>Download</a>
      </td>
    </tr>
  </tbody>
</table>

## Get started

{/* eslint-disable-next-line @next/next/no-img-element */}

<img
  src="/images/docs/desktop-start.png"
  alt="Omnigent desktop app first-launch setup screen"
  style={{ width: "100%", borderRadius: "8px" }}
/>

1. **Launch the app.** On first launch, you'll see a setup screen.
2. **Enter your server URL.** `http://localhost:6767` if running locally, or your deployed server URL (e.g. `https://your-app.onrender.com`). For a Databricks workspace URL, the app signs you in through your system browser — see [Sign in to a Databricks workspace](#sign-in-to-a-databricks-workspace).
3. **Start working.** The app connects and shows the same UI you'd see in a browser.

{/* eslint-disable-next-line @next/next/no-img-element */}

<img
  src="/images/docs/desktop-conversation.png"
  alt="Omnigent desktop app showing an agent conversation alongside the session workspace"
  style={{ width: "100%", borderRadius: "8px" }}
/>

The app remembers your server URL for future launches. To change it later, go to
**Server > Change Server**.

## Work with multiple servers

Each window connects to one server. To work with multiple servers at the same time:

1. Go to **Server > New Window on Different Server**
2. Enter the URL of the second server

Notifications and badge counts are combined across all connected servers.

{/* TODO(doc-drafter): screenshot may be stale — the Connect button now shows Connecting… / Authenticating… states with a trailing × cancel button during Databricks browser sign-in. */}
{/* eslint-disable-next-line @next/next/no-img-element */}

<img
  src="/images/docs/desktop-connect.png"
  alt="Omnigent desktop app setup screen prompting for the server URL to connect to"
  style={{ width: "100%", borderRadius: "8px" }}
/>

## Sign in to a Databricks workspace

When the server URL is an `https://` Databricks workspace or account URL (a host
under `databricks.com` or `azuredatabricks.net`), the desktop app signs you in
through your **system browser** using OAuth instead of showing the workspace's
login page inside the app window. Databricks Apps (`*.databricksapps.com`),
`localhost`, and every other server keep their existing sign-in behavior.

On the connect screen, the **Connect** button shows **Connecting…** and then
**Authenticating…** while your browser opens the Databricks sign-in page. Once
you finish signing in, the desktop creates the workspace session and loads
Omnigent. If you entered an account URL, the app shows a workspace picker so you
can choose which workspace to open.

- **Cancel.** The **×** button next to **Authenticating…** cancels the current
  attempt and returns the form to **Connect** with the same URL filled in, so
  you can retry. The tab in your external browser may stay open; cancelling
  does not sign you out of that browser.
- **No automatic browser pop-ups.** Reopening a saved server, opening another
  window, switching servers, and following an `omnigent://` link all reuse your
  stored credentials without launching a browser.
- **Session renewal.** The app renews the workspace session in the background
  before it expires. If renewal isn't possible — missing credentials, OAuth
  unavailable, or a cancelled sign-in — you're returned to the connect screen
  rather than an embedded login page. Choose **Connect** to sign in through the
  browser again.

### Roll back to embedded sign-in (macOS)

If you need the previous behavior, where the Databricks login page is shown
inside the app window, you can switch the packaged macOS app back with a user
default. Quit Omnigent first, then:

```bash
defaults write ai.omnigent.desktop DatabricksBrowserAuthEnabled -bool false
```

This selects the whole legacy flow — no system-browser OAuth and no
OAuth-based session renewal. It is an explicit switch, not an automatic
fallback after a failed browser sign-in. The preference is read once when the
app starts, so restart Omnigent after changing it. It does not delete saved
credentials.

To return to browser sign-in, quit Omnigent and remove the override:

```bash
defaults delete ai.omnigent.desktop DatabricksBrowserAuthEnabled
```

An unset key (or an explicit `-bool true`) means browser sign-in. The
preference is honored only on macOS; Linux and Windows always use browser
sign-in.

### Troubleshooting sign-in

Launch the app's executable from a terminal and look for lines prefixed with
`[omnigent] databricks`. No extra logging flag is needed. The logs show the
selected sign-in mode, the OAuth client id and requested scopes, token-exchange
status, account/workspace routing, and the final response status, plus
Databricks error codes and request ids when available. Tokens, cookie values,
client secrets, authorization codes, and raw response bodies are never logged.
Workspace and client ids are still deployment metadata, so redact them before
sharing logs. For a direct `403 PERMISSION_DENIED`, give the request id and
client id to your Databricks platform owner to check the denial reason (client
enablement, token scope, or workspace permissions).

## Managed servers (macOS MDM)

On macOS, administrators can publish a list of server URLs through MDM
[Managed Preferences](https://support.apple.com/guide/deployment/welcome/web),
so people can pick their organization's server instead of typing it. The
preference domain is the desktop app's bundle identifier:

```text
ai.omnigent.desktop
```

Set the `serverUrls` key to an array of server URLs, most-preferred first:

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Type</th>
      <th>Required</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>serverUrls</code>
      </td>
      <td>Array of strings</td>
      <td>No</td>
      <td>
        <code>[]</code>
      </td>
      <td>
        Server URLs to offer, most-preferred first. Each must use{" "}
        <code>https://</code>. At most 10.
      </td>
    </tr>
  </tbody>
</table>

A schemeless host is accepted and interpreted as `https://`. Paths are
preserved, so an administrator can point directly at a workspace mount such as
`https://my-workspace.cloud.databricks.com/ml/omnigents`. Entries that share an
origin are collapsed, keeping the first. If any entry is an invalid type,
insecure, or malformed — or the list has more than 10 entries — the whole list
is rejected.

Managed servers are read only on macOS; other platforms ignore the preference.

### How they appear

Managed servers show up under **Provided by your organization** on the connect
screen and in the in-app server switcher. They are offered, not enforced:

- Omnigent never connects to them automatically.
- People can still type another server URL.
- A managed workspace path is loaded exactly as configured, rather than being
  reduced to its origin.
- Values are read from macOS on demand, not copied into the app's settings, so
  applying or removing a profile takes effect the next time the connect screen
  loads or the server switcher opens.

### Example MDM profile

Use the standard `com.apple.ManagedClient.preferences` payload with the
`ai.omnigent.desktop` application preference domain (most MDM products expose
this as a custom settings or managed preferences payload):

```xml
<key>ai.omnigent.desktop</key>
<dict>
  <key>Forced</key>
  <array>
    <dict>
      <key>mcx_preference_settings</key>
      <dict>
        <key>serverUrls</key>
        <array>
          <string>https://omnigent.corp.example.com</string>
          <string>https://my-workspace.cloud.databricks.com/ml/omnigents</string>
        </array>
      </dict>
    </dict>
  </array>
</dict>
```

{/* eslint-disable-next-line @next/next/no-img-element */}

<img
  src="/images/docs/desktop-connect.png"
  alt="Omnigent desktop app setup screen prompting for the server URL to connect to"
  style={{ width: "100%", borderRadius: "8px" }}
/>

## Open a session from a link

The desktop app registers the `omnigent://` URL scheme, so a link can open a
specific session on a specific server the way a web link opens a page. The link
names the server by host and the session by its conversation id:

```
omnigent://localhost:6767/c/conv_abc
omnigent://my-workspace.cloud.databricks.com/c/conv_xyz
```

The link carries no `http`/`https` — the app infers it the same way the setup
screen does: `http` for a loopback host (`localhost`, `127.0.0.1`), `https` for
a remote host. A Databricks workspace mount is discovered automatically, so you
don't include it in the link. Only `/c/<session_id>` links are recognized;
other paths are ignored.

Clicking a link behaves like opening a page for a known site:

- If a window is already connected to that server, the app **reuses it** and
  navigates to the session in place.
- If you've connected to that server before but have no window open for it, the
  app opens it in a **new window**.
- If you've **never connected** to that server, the app asks you to confirm
  first, since connecting to a server grants it privileges like notifications
  and registering this machine as a runner. Cancel is the default; only open
  links from servers you trust. Once you allow it, the server is remembered so
  future links open without a prompt.
