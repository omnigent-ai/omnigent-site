import { pageMeta } from "@/lib/og";

export const metadata = pageMeta("Harnesses", "The harness is the runtime that executes your agent loop. Swap one line of YAML to switch between Claude, Codex, and other runtimes.", {
  eyebrow: "Build",
  path: "/docs/build/harnesses",
});

# Harnesses

The harness is the runtime that executes your agent loop. Swap one line to switch runtimes.

```
executor:
  harness: claude
```

## Supported harnesses

| Harness | Runtime | Best for |
| --- | --- | --- |
| `claude` | Claude Code | Claude Code out of the box. Add tools, policies, and prompts to customize. |
| `codex` | Codex | Codex out of the box. Add tools, policies, and prompts to customize. |
| `pi` | Pi | Headless multi-model worker that runs on any gateway model. Ideal for review, exploration, and read-heavy tasks delegated by a supervisor agent. |

More harness integrations are on the way.

## Optional & community harnesses

`pip install omnigent` ships the built-in harnesses above. Additional harnesses
are distributed as optional packages that plug into the **same** `omni` CLI and
server — no separate binary or process. For example:

```
pip install omnigent-kimi
```

adds its harness ids to spec validation, the harness picker, and readiness
checks the moment it's installed.

Under the hood, an optional package registers a contribution in the
`omnigent.community.harnesses` entry point group. Omnigent discovers installed
plugins at startup and merges their harness ids, aliases, install metadata, and
picker labels with the core set.

If you reference a harness that isn't installed, validation reports the accepted
harnesses. For a known optional harness, the error also tells you which package
to install:

```
executor.config.harness: must be one of [...], got 'kimi'; install `omnigent-kimi` to add this harness
```

The server exposes the currently installed harnesses at `GET /v1/harnesses`, and
the web UI's harness pickers read from it so newly installed harnesses appear
automatically.

## Swap harnesses

Tools, policies, and other config stay the same across harnesses. Just change the
`harness` value in your YAML, or override it at runtime:

```
omni run agent.yaml --harness codex
```

See [Models & Credentials](/docs/build/models) for how to set up API keys and choose models.
